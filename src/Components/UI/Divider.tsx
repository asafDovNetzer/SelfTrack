const Divider = () => {
  return (
    <div
      style={{
        width: `100%`,
        height: `1px`,
        backgroundImage: `linear-gradient(to right, white, grey, white)`,
      }}
    ></div>
  );
};

export default Divider;
